// Owner: Person 2 (transaction layer).
// STATUS: stub only — no working logic yet.
//
// SuiNS registration for agents (human-readable identity, e.g.
// "my-agent.sui") — referenced by AgentIdentity.suins_name.
// VERIFY exact SuiNS SDK package name and registration flow against
// current SuiNS docs before implementing.

// TODO: export async function registerAgentName(name: string, ownerAddress: string): Promise<...>
// TODO: export async function resolveAgentName(name: string): Promise<string | null>
