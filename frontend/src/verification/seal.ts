// Owner: Person 3 (verification/storage).
// STATUS: stub only — no working logic yet.
//
// Seal encryption/access-policy calls: encrypts sensitive negotiation or
// deliverable content so only the client and specialist agent (and any
// on-chain-defined policy) can decrypt it. VERIFY exact @mysten/seal (or
// equivalent) API — key server endpoints, encrypt/decrypt function names,
// access-policy object shape — against current Seal docs before
// implementing.

// TODO: export async function encryptForDeal(dealId: string, plaintext: Uint8Array): Promise<Uint8Array>
// TODO: export async function decryptForDeal(dealId: string, ciphertext: Uint8Array): Promise<Uint8Array>
