// Owner: Person 4 (frontend + orchestration).
// STATUS: stub only.
//
// Plain-language goal input — the entry point where a human tells Envoy
// what they want done. Feeds into src/agent's discovery/matching logic.

// TODO: export function GoalInput(props: { onSubmit: (goal: string) => void }): JSX.Element
