// Owner: Person 4 (frontend + orchestration).
// STATUS: stub only.
//
// TODO: render <App /> from ./app into #root via createRoot. Wire up any
// top-level providers here (wallet provider, zkLogin context from
// Person 2's src/sui, etc.) once those are built — do not invent their
// shape yet, coordinate with Person 2.
