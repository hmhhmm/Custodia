// Owner: Person 4 (frontend + orchestration).
// STATUS: stub only — no working logic yet.
//
// Scripted specialist-agent stand-ins for the demo: simulate a specialist
// agent's negotiation replies and delivered-work output so the full flow
// can be demoed without needing multiple real autonomous agents running
// live. Clearly label these as scripted/demo-only wherever used — do not
// let them appear indistinguishable from a real counterparty agent.

// TODO: export function scriptedSpecialistReply(context: unknown): string
// TODO: export function scriptedDeliverable(context: unknown): { content: string }
