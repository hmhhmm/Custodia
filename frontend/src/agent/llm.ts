// Owner: Person 4 (frontend + orchestration).
// STATUS: stub only — no working logic yet.
//
// LLM call layer for Envoy: turns plain-language goals into structured
// intents, drafts negotiation messages, and interprets specialist-agent
// responses. VERIFY exact API/SDK details (model name, endpoint, auth)
// against official provider docs before implementing — do not assume a
// specific model id or pricing without checking current docs.

// TODO: export async function interpretGoal(goal: string): Promise<{
//   category: string;
//   maxBudget: number;
//   description: string;
// }>

// TODO: export async function draftNegotiationMessage(context: unknown): Promise<string>
